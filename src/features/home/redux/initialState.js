const initialState = {
  isLoading: false,
  isError: false,
  redirect:false,
  redirectTo:'/',
  items: {}, // Products Info Directive
  myBox: {}, // Detailed Selected Products Info
};

export default initialState;
